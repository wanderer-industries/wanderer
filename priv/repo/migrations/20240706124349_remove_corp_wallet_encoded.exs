defmodule WandererApp.Repo.Migrations.RemoveCorpWalletEncoded do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:corp_wallet_transactions_v1) do
      remove :encrypted_second_party_id_encoded
      remove :encrypted_first_party_id_encoded
      add :first_party_id, :bigint, null: false
      add :second_party_id, :bigint, null: false
      add :first_party_id_encoded, :bigint, null: false
      add :second_party_id_encoded, :bigint, null: false
    end
  end

  def down do
    alter table(:corp_wallet_transactions_v1) do
      remove :second_party_id_encoded
      remove :first_party_id_encoded
      remove :second_party_id
      remove :first_party_id
      add :encrypted_first_party_id_encoded, :binary, null: false
      add :encrypted_second_party_id_encoded, :binary, null: false
    end
  end
end
