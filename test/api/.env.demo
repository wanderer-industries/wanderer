# Demo API Test Environment Variables
# This file shows what a properly configured .env should look like

# ⚠️  IMPORTANT: These are EXAMPLE values that won't work!
# You need to create a real map and get real credentials.

# Example of what a real API token looks like (this one is fake)
API_TOKEN=a1b2c3d4-e5f6-7890-abcd-ef1234567890

# Example of what a map slug looks like
# This comes from your map's URL: https://wanderer.example.com/map/my-corp-chain
# In this case, the slug would be: my-corp-chain
MAP_SLUG=my-corp-chain

# Optional: ACL testing (leave empty if not testing ACLs)
ACL_API_TOKEN=
ACL_ID=

# Base URL (change if testing against a different server)
API_BASE_URL=http://localhost:4000

# Test data (these are real EVE system IDs)
TEST_CHARACTER_ID=95000001
TEST_SYSTEM_ID_1=30000142  # Jita
TEST_SYSTEM_ID_2=30000144  # Perimeter