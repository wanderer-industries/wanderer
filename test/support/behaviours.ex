# Define behaviours at the top level to avoid module nesting issues
# PubSub behaviour is defined in lib/wanderer_app/test/pubsub.ex
# Logger behaviour is defined in lib/wanderer_app/test/logger.ex
# DDRT behaviour is defined in lib/wanderer_app/test/ddrt.ex
